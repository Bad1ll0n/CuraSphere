#!/bin/sh
# Block commits that include .env files with secrets
if git diff --cached --name-only | grep -qE '(^|/)\.env$'; then
  echo "❌ ERRO: Tentativa de commit de ficheiro .env com segredos!"
  echo "   Use .env.example para valores de exemplo."
  exit 1
fi
